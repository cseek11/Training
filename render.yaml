# render.yaml

services:
  - type: web
    name: my-backend
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: my-database
          property: connectionString

